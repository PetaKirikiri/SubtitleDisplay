/**
 * Timeline Navigation - Main Exports
 * 
 * Simple player control interface:
 * - getCurrentTime() - Get current video time
 * - seekToTime(timeSeconds) - Seek to specific time
 * - pause() - Pause video
 * - unpause() - Unpause video
 */

export { getCurrentTime, seekToTime, pause, unpause } from './playerControl';
